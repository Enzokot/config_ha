
    - trigger:
        - trigger: state
          entity_id: binary_sensor.0x158d0003cfa725_contact
        - trigger: homeassistant
          event: start
        - trigger: event
          event_type: event_template_reloaded
      binary_sensor:
        - name: lobby_door_open_long_time
          state: "{{ is_state('binary_sensor.0x158d0003cfa725_contact', 'on') }}"
          delay_on:
            minutes: 5
          delay_off:
            seconds: 10

#Просроченные сертификаты ssl
    - trigger:
        - trigger: state
          entity_id: sensor.cert_expiry_netcraze_domain
        - trigger: homeassistant
          event: start
        - trigger: event
          event_type: event_template_reloaded
      binary_sensor:
        - name: cert_expired_netcraze_domain
          state: "{{ states('sensor.cert_expiry_netcraze_domain') | int(0) <= 5 }}"
          availability: "{{ not is_state('sensor.cert_expiry_netcraze_domain', 'unavailable') }}"
          delay_on:
            minutes: 1
          delay_off:
            minutes: 1

    - trigger:
        - trigger: state
          entity_id: sensor.cert_expiry_asuscomm_domain_klg
        - trigger: homeassistant
          event: start
        - trigger: event
          event_type: event_template_reloaded
      binary_sensor:
        - name: cert_expired_asuscomm_domain_klg
          state: "{{ states('sensor.cert_expiry_asuscomm_domain_klg') | int(0) <= 5 }}"
          availability: "{{ not is_state('sensor.cert_expiry_asuscomm_domain_klg', 'unavailable') }}"
          delay_on:
            minutes: 1
          delay_off:
            minutes: 1
